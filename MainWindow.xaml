<Window x:Class="GradescopeIOViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradescopeIOViewer"
        mc:Ignorable="d"
        Title="L's Gradescope I/O Viewer" Height="450" Width="800">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Grid.Row="0" x:Name="btnOpenFolder" Margin="0,5,0,0" Click="ButtonOpenFolder_Click">Open Folder</Button>

        <Label Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="0" Name="folderLabel">
            No folder selected            
        </Label>

        <ListBox Grid.Column="0" Grid.Row="1" x:Name="CasesBox" Width="200" Height="Auto" SelectionChanged="OpenCase" SelectionMode="Single">
            
        </ListBox>

        <TextBox Grid.Column="1" Grid.Row="1" Name="inputText" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            
        </TextBox>

        <TextBox Grid.Column="2" Grid.Row="1" Name="outputText" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            
        </TextBox>

        <Label Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2">
            "L's Gradescope I/O Viewer" is in no way associated with Gradescope or Turnitin, LLC.
        </Label>

    </Grid>
</Window>
